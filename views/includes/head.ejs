<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/css/Navigation-with-Button.css">
    <link rel="stylesheet" href="assets/css/styles.css">
</head>

<body>
    <nav class="navbar navbar-light navbar-expand-md navigation-clean-button">
        <div class="container"><a class="navbar-brand" href="/">Alpaca Airways</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse"
                id="navcol-1">
                <ul class="nav navbar-nav mr-auto">
                <% if (isAuth) { %>
                    <li class="nav-item" role="presentation"><a class="nav-link" href="flights">Upcoming Flights</a></li>
                    <li class="nav-item" role="presentation"><a class="nav-link" href="booked">Booked Flights</a></li>
                <% } %>
                </ul>
                <span class="navbar-text actions">
                    <% if (isAdmin) { %>
                    <a class="login" href="#">Admin</a>
                    <% } if (!isAuth) { %>
                    <a class="btn btn-light action-button" role="button" href="https://discord.com/api/oauth2/authorize?client_id=707334427398635570&redirect_uri=http%3A%2F%2Flocalhost%2Flogin&response_type=code&scope=identify%20guilds">Sign In</a>
                    <% } else { %>
                    <a class="btn btn-light action-button" role="button" href="logout">Sign Out</a>
                    <% } %>
                </span></div>
        </div>
    </nav>
    <div class="card">
        <div class="card-body">
            <% if (error) { %>
                <div class="user-message user-message-error"><%= error %></div>
            <% } %>
            <% if (notification) { %>
                <div class="user-message"><%= notification %></div>
            <% } %>    
